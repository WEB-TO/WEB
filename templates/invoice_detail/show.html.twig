{% extends 'base.html.twig' %}

{% block title %}InvoiceDetail{% endblock %}

{% block body %}
    <h1>Invoice Detail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invoice_detail.id }}</td>
            </tr>
            <tr>
                <th>Invoice Id</th>
                <td>{{ invoice_detail.invoice.id }}</td>
            </tr>
            <tr>
                <th>Product Id</th>
                <td>{{ invoice_detail.product.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_invoice_detail_index') }}">back to list</a>

    <a href="{{ path('app_invoice_detail_edit', {'id': invoice_detail.id}) }}">edit</a>

    {{ include('invoice_detail/_delete_form.html.twig') }}
{% endblock %}
